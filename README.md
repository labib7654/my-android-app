# DistanceCalculator

A minimal Android app that tracks location changes and shows total distance traveled.

## What is included
- Android source files
- Gradle build files
- GitHub Actions workflow for APK build

## Important
The workflow is set up to install the Android SDK tools and a Gradle version on GitHub Actions.
If you change the package name or app name, update the matching files in `app/src/main`.
